{
  "name": "@jsweb/truetype",
  "version": "3.0.9",
  "description": "Simple JS module to check types more consistently",
  "scripts": {
    "test": "run-s test:*",
    "test:lint": "npm run lint",
    "test:clean": "shx rm -rf mocha*",
    "test:unit": "mocha src/**/*.test.ts --require ts-node/register --reporter mochawesome --reporter-options reportTitle=@jsweb/truetype",
    "major": "npm version major && npm run push",
    "minor": "npm version minor && npm run push",
    "patch": "npm version patch && npm run push",
    "prepare": "npm run dist",
    "push": "git push && git push --tags",
    "lint": "tsc --noEmit && tslint -p . -c tslint.json --fix",
    "dist": "run-s dist:*",
    "dist:clean": "shx rm -rf dist",
    "dist:test": "npm t",
    "dist:build": "rollup -c",
    "dist:types": "tsc --removeComments --emitDeclarationOnly"
  },
  "author": "Alex Bruno <git.alexbr@outlook.com>",
  "license": "MIT",
  "keywords": [
    "javascript",
    "type",
    "test",
    "check"
  ],
  "homepage": "https://truetype.jsweb.app",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jsweb/truetype.git"
  },
  "bugs": {
    "url": "https://github.com/jsweb/truetype/issues"
  },
  "devDependencies": {
    "@types/mocha": "^5.2.5",
    "mocha": "^5.2.0",
    "mochawesome": "^3.0.3",
    "npm-run-all": "^4.1.5",
    "rollup": "^1.0.1",
    "rollup-plugin-esmin": "^0.1.3",
    "rollup-plugin-typescript": "^1.0.0",
    "shx": "^0.3.2",
    "ts-node": "^7.0.1",
    "tslib": "^1.9.3",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  },
  "main": "dist/umd.js",
  "esnext": "dist/module.js",
  "module": "dist/module.js",
  "types": "dist/module.d.ts"
}
